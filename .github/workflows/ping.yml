name: Keep Render awake
on:
  schedule:
    - cron: '*/10 * * * *'   # every 10 minutes (UTC)
  workflow_dispatch: {}
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping site health endpoint
        run: curl -fsS https://radhika-agencies.onrender.com/api/health || true
